import 'package:flutter/material.dart';

class PhotographingWithCellphonePage extends StatelessWidget {
  const PhotographingWithCellphonePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Fotografando com o celular'),
      ),
      body: const Center(
        child: Text('Fotografando com o celular'),
      ),
    );
  }
}

