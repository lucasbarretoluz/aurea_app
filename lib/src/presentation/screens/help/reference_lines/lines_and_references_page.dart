import 'package:flutter/material.dart';

class LinesAndReferencesPage extends StatelessWidget {
  const LinesAndReferencesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Linhas e referências'),
      ),
      body: const Center(
        child: Text('Linhas e referências'),
      ),
    );
  }
}

