import 'package:flutter/material.dart';

class DentalProportionsPage extends StatelessWidget {
  const DentalProportionsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Proporções dentárias'),
      ),
      body: const Center(
        child: Text('Proporções dentárias'),
      ),
    );
  }
}

